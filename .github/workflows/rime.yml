name: Rime
on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        path: 
          - fffffx
          - unions
          - x-word-database
        
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rime
        run: pip install rime

      - name: Rime test
        run: rime test ${{ matrix.path }} -k
