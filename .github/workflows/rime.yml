name: Rime
on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        path: 
          - straight-conditions
          - fffffx
          - unions
          - x-word-database
          - lcm-and-gcd
          - range-rotate-query
        
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rime
        run: pip install rime

      - name: Rime test
        run: rime test ${{ matrix.path }} -k
